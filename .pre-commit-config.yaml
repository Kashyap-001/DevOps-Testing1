repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0  # Use the latest version
    hooks:
      # Runs the linter
      - id: ruff
        args: [ --fix ]
      # Runs the formatter (replaces Black)
      - id: ruff-format